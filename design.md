# 青岛·八大关 —— three.js 互动虚拟景观 设计方案

> 本文档是完整的实现蓝图。目标读者是负责实现的 coding agent：所有关键决策（坐标、配色、参数、模块划分）都已给出具体数值，实现时**不需要再做额外调研**，照此执行即可。配套的分阶段任务清单见 [todo.md](./todo.md)。

---

## 1. 项目概述

**做什么**：一个可以在浏览器里第一人称漫步的「青岛八大关」虚拟景区。玩家用鼠标+键盘（WASD）自由行走、旋转视角，场景通过光影、海雾、声音传达八大关的真实氛围——「红瓦绿树、碧海蓝天」、万国建筑、一关一树、海滨礁石。

**不做什么**：不追求测绘级还原（街区布局做了艺术化压缩）、无任务/剧情系统、无多人、不依赖任何外部 3D 模型资产（全部程序化生成，保证零下载即可构建运行）。

**成功标准**（验收时逐条核对，详见 §10）：
1. 首屏加载 < 3 秒，桌面中端 GPU 上稳定 60 FPS；
2. 站在花石楼海岬平台上：能看到夕阳下海面的高光反射、听到浪声、海鸥掠过；
3. 走进居庸关路：金黄银杏夹道、落叶飘落，与其他路的树种明显不同；
4. 切换到「海雾」模式：能见度骤降、路灯光晕亮起，氛围完全改变；
5. 任何一帧画面截图给青岛人看，能认出「这是八大关」。

---

## 2. 场地调研结论（真实性依据）

以下事实来自公开资料核实（来源见 §12），是场景设计的「真实性锚点」，实现时不可随意更改：

### 2.1 总体印象
- 八大关位于青岛市南区南部海滨，是约十条以中国著名关隘命名的道路组成的别墅区，南临黄海、第二海水浴场。
- 康有为对青岛的著名概括「**红瓦绿树、碧海蓝天**」是本场景的核心色彩纲领。
- 汇集俄、英、法、德、美、日、丹麦等 20 多国建筑风格的 200 余栋别墅，人称「**万国建筑博览会**」。
- 街区特质：**安静**。车少人稀、绿篱矮墙、花岗岩院墙+铁艺门、欧式路灯，是著名婚纱摄影地。
- 青岛春夏常见**平流海雾**，是当地极具辨识度的天气现象——本项目将其做成可切换的氛围模式。

### 2.2 「一关一树」——各路行道树（已核实，必须按此映射）
| 道路 | 行道树 | 秋季（本场景季节）表现 |
|---|---|---|
| 韶关路 | 碧桃 | 小乔木，秋季叶橙绿（春季开花，本场景不体现花） |
| 正阳关路 | 紫薇 | 小树，秋叶橙红 |
| 居庸关路 | **银杏** | **金黄色隧道，本场景最强视觉记忆点** |
| 嘉峪关路 | **五角枫** | 红/橙红色 |
| 临淮关路 | 龙柏 | 常绿深绿，柱状 |
| 宁武关路 | 海棠 | 小乔木，秋叶黄绿带橙 |
| 紫荆关路 | **雪松** | 常绿，高大塔形，深绿 |
| 山海关路 | 法桐（悬铃木） | 高大，秋叶黄褐 |
| 海滨/岬角 | 黑松（青岛海岸标志树） | 常绿深绿，姿态虬曲 |

### 2.3 地标建筑（已核实）
| 建筑 | 位置 | 特征（建模依据） |
|---|---|---|
| **花石楼** | 黄海路18号，紫荆关路南端海岬，三面临海 | 1930年代欧洲古堡式：**原色花岗岩砌墙**、转角蘑菇石、局部鹅卵石贴面，**圆形+多角形组合塔楼**，塔顶**雉堞式女儿墙**（城堡垛口），哥特尖拱窗。蒋介石曾居于此。本场景第一地标 |
| **公主楼** | 居庸关路10号 | 丹麦童话风格：**墨绿色外墙**、陡峭双坡屋顶+方形塔楼、白色窗框，精巧小楼 |
| **蝴蝶楼** | 山海关路21号 | 折衷主义别墅，1934年，因影星胡蝶在此拍片得名 |
| **元帅楼** | 山海关路17号 | 近代和风（日式）二层小楼，多位元帅曾下榻 |
| **第二海水浴场** | 山海关路以南海滨 | 沙滩+东端礁石群（礁石紧邻花石楼海岬） |

### 2.4 普通别墅的共性特征（程序化生成的参数依据）
- 2–3 层，砖石结构；**红色筒瓦四坡/双坡屋顶**（青岛红瓦是最强符号）；
- 外墙：米黄、奶白、淡粉、浅赭色拉毛水泥墙面（少量花岗岩清水墙）；
- **花岗岩蘑菇石墙基/转角**、白色窗框、部分木百叶、拱形门洞、烟囱、小阳台；
- 院落：约 0.9–1.2 m 高**花岗岩矮墙 + 绿篱**围合，铁艺大门，院内有庭院树。

---

## 3. 艺术方向（Art Direction）

### 3.1 时刻与季节
- **季节固定为深秋（10月末–11月初）**：银杏金黄、五角枫火红、雪松黑松常绿——色彩层次最丰富，且是八大关最著名的观赏季。
- **默认时刻：黄昏黄金时段（golden hour）**，太阳位于西南方低角度（仰角约 10°），暖光斜射、长影子，红瓦被点燃、海面出现日光带。
- 提供 3 个可切换的**氛围预设**（键盘 1/2/3）：

| 预设 | 太阳仰角/方位 | 雾 | 关键氛围 |
|---|---|---|---|
| ① 黄昏（默认） | 仰角 10°，方位西南 225° | FogExp2 密度 0.0032，暖橙灰 `#c8a288` | 金光、长影、海面日光带、路灯刚点亮 |
| ② 晴午 | 仰角 55°，方位 200° | 密度 0.0018，淡蓝白 `#cfe0e8` | 「红瓦绿树碧海蓝天」标准色，高饱和 |
| ③ 海雾 | 仰角 25°（被雾吞没） | 密度 0.012，冷灰 `#b8c4c6` | 能见度约 80 m，路灯光晕全亮，声音以雾号/浪声为主 |

### 3.2 色彩脚本（Color Script）——全局调色板常量（写入 `config.js`）
```js
export const PALETTE = {
  roofRed:      ['#a83f2c', '#b5502f', '#933526', '#c05a35'],   // 红瓦（4档随机）
  wallWarm:     ['#e8d9b0', '#f0e6cc', '#e6c9a8', '#e9d5c3', '#dfc9b4'], // 米黄/奶白/淡粉墙面
  granite:      '#9a938a',  graniteDark: '#7d766e',             // 花岗岩/蘑菇石
  princessGreen:'#3f6151',                                       // 公主楼墨绿
  windowFrame:  '#f5f2ea',  glassDusk: '#ffd9a0', glassDay: '#8fb6c8',
  ginkgo:       '#f2c14e',  mapleRed: '#c8502e',  planeTree: '#c9a24b',
  cedar:        '#2e4a34',  blackPine: '#26382b', peach: '#7d9b4e',
  crapeMyrtle:  '#b7793f',  crabapple: '#a8913f',
  grass:        '#4a6b3a',  hedge: '#395430',
  road:         '#5c5850',  sidewalk: '#8f887c', curb: '#a29a8c',
  sand:         '#d9c49a',  rock: '#5a564f',
  seaDeep:      '#1a4f63',  seaShallow: '#2e7d8c',
  lampGlow:     '#ffc773',
};
```
- 渲染基调：ACES 电影色调映射 + 轻微 Bloom，黄昏模式整体偏暖 2%（通过雾色与太阳色实现，不加全屏滤镜）。

### 3.3 声音氛围（全部 WebAudio 程序化合成，零音频资产）
| 声源 | 合成方式 | 空间化 |
|---|---|---|
| 海浪 | 粉噪声 + 低通(400→900Hz LFO 摆动) + 8–12s 周期的增益包络（涌—碎—退） | 音量随「玩家到海岸线距离」衰减：z>40 全音量，z<-60 几乎不可闻 |
| 风/树叶沙沙 | 白噪声 + 带通(1.2kHz) + 慢随机增益，树密集区略增 | 全局，微弱 |
| 海鸥 | 2 个振荡器扫频「啾—啾」样本化短句，随机 15–40s 触发一次 | 仅海滨区触发，随机声像 |
| 雾号（仅海雾模式） | 110Hz 方波+低通，2s 长鸣，每 45s 一次，极低音量 | 从东南方向 |
- 遵守浏览器自动播放策略：首次点击「开始游览」后才创建 `AudioContext`。

---

## 4. 场景布局（World Map）

### 4.1 坐标系与尺度
- 单位 = 米。`+x` 东，`-x` 西；`+z` 南（海侧），`-z` 北（内陆）。`y` 向上。
- 世界范围：`x ∈ [-190, 190]`，`z ∈ [-170, 150]`（约 380 m × 320 m，真实街区约 700×500 m，做了约 55% 压缩，保留全部地标相对关系）。
- **地面高程（刻意简化为三段，便于碰撞与摆放）**：
  - 城区平台：`y = 2.5`（`z ≤ 75` 的全部区域）；
  - 海滩坡道：`z ∈ [75, 100]`，`y` 从 2.5 线性降至 -0.4（沙滩没入水）；
  - 海面：`y = 0` 的大平面（Water），覆盖 `z > 88` 至远方及东西两侧延伸至雾中；
  - 花石楼海岬：一块凸出的岩台，顶面 `y = 4.0`，见 §4.3。
  - 实现一个唯一的高度查询函数 `getGroundHeight(x, z)`，玩家控制、树木、建筑摆放全部调它，**杜绝各模块自算高度**。

### 4.2 道路网（写入 `config.js` 的数据表，道路=沿折线挤出的条带）
东西向（横）：
| 路名 | 折线（x,z 序列） | 宽度 | 行道树 |
|---|---|---|---|
| 武胜关路 | (-170,-75) → (170,-75) | 7 | 龙柏 |
| 正阳关路 | (-170,-10) → (170,-10) | 8 | 紫薇 |
| 山海关路 | (-170,55) → (170,55) | 7 | 法桐 |
| 黄海路 | (-60,85) → (40,85) → (58,92)（接花石楼岬） | 6 | 黑松（稀疏） |

南北向（纵）：
| 路名 | 折线 | 宽度 | 行道树 |
|---|---|---|---|
| 韶关路 | (-120,-160) → (-124,-75) → (-118,-10) → (-120,55)（微曲） | 6 | 碧桃 |
| 宁武关路 | (-60,-160) → (-60,55) | 6 | 海棠 |
| 居庸关路 | (0,-160) → (0,55) | 7 | **银杏** |
| 紫荆关路 | (60,-160) → (60,55) → (58,92)（南端接黄海路弯向花石楼） | 6 | 雪松 |
| 嘉峪关路 | (115,-160) → (115,55) | 6 | 五角枫 |
- 路面结构：车行道（深沥青 `road` 色，中央无标线——八大关实景就没有）+ 两侧 1.5 m 人行道（`sidewalk`，抬高 0.12 m）+ 路缘石。交叉口自动求并（简化：先铺纵路再铺横路，同高程 z-fighting 用 `polygonOffset` 或 0.01 错层解决）。

### 4.3 地标与分区（具体坐标）
```
                        北 (-z)
   ┌────────────────────────────────────────────┐ z=-170
   │   别墅街区（韶关/宁武/居庸/紫荆/嘉峪 五纵）      │
   │  ══════════ 武胜关路 z=-75 ══════════        │
   │   别墅街区   ▣公主楼(4,-42)                   │
   │  ══════════ 正阳关路 z=-10 ══════════        │
   │   ▣蝴蝶楼(-32,48)  ▣元帅楼(-12,48)           │
   │  ══════════ 山海关路 z=55 ═══════════        │
   │   海滨草坡+黑松     ── 黄海路 z=85 ──          │
   │ ~~ 第二海水浴场沙滩 ~~      ▲花石楼海岬(60,100) │
   │ ~~~~~~~~~~~ 海  面 (y=0) ~~~~~~~~~~~~~~~~~  │
   └────────────────────────────────────────────┘ z=150
  x=-190                                      x=190
```
- **花石楼**：中心 `(60, 100)`，坐落于岩石海岬（岩台顶 `y=4`，向海面以乱石跌落，`z` 最远伸到 118）。朝向：正面（主入口）朝北偏西对着紫荆关路来向。周围一圈石砌观景平台+白色宝瓶栏杆。**礁石群**散布在岬角东、南两侧水中（`x∈[45,95], z∈[105,135]`，8–14 块）。
- **第二海水浴场**：沙滩 `x ∈ [-150, 35]`，`z ∈ [75, 105]`。沙滩上放 3–4 组更衣棚剪影（简单彩色条纹小屋）与零星礁石在西端。
- **公主楼** `(4, -42)`，居庸关路东侧，朝西。
- **蝴蝶楼** `(-32, 48)`、**元帅楼** `(-12, 48)`：山海关路北侧，朝南。
- **普通别墅**：约 32 栋，填充各街区。生成规则：沿每条路两侧、距路缘 8–14 m，间距 22–35 m，随机避开地标 20 m 半径；每栋带院墙（花岗岩矮墙 h=1.0 + 绿篱 h=1.3，临街开 3 m 门口+铁艺门）、院内 1–2 棵庭院树。别墅位置在 `config.js` 里**用种子随机预生成后固化成静态数组**（保证每次运行场景一致、可调试）。
- **出生点**：`(0, -55)`，位于居庸关路上、公主楼北侧，面朝正南（初始视线穿过银杏隧道、尽头是海）。这是精心选择的「第一眼镜头」。

### 4.4 推荐游览动线（用于摆放兴趣点与触发提示）
出生（银杏隧道）→ 南行遇公主楼（左手）→ 过正阳关路 → 山海关路口见蝴蝶楼/元帅楼 → 继续南下上黄海路 → 花石楼海岬观海 → 沿沙滩西行 → 从宁武关路或韶关路折返北上。沿线 6 个「地点提示」触发器见 §7.5。

---

## 5. 技术架构

### 5.1 技术选型（已定，勿改）
| 项 | 选择 | 理由 |
|---|---|---|
| 渲染库 | **three@^0.185.0**（2026-06 最新版） | 使用 `WebGLRenderer`（不用 WebGPURenderer——兼容性优先；本场景瓶颈不在 API） |
| 构建 | **Vite 7**（`npm create vite@latest`，vanilla 模板） | 零配置、dev 快 |
| 语言 | **原生 JavaScript (ES Modules) + JSDoc 类型注释** | 无 TS 编译层，降低实现摩擦 |
| 依赖 | 仅 `three`。addons 用 `three/addons/*` 路径导入 | 不引 nipplejs/tweakpane 等任何第三方 |
| 资产 | **零外部资产**：贴图全部用 Canvas 程序化生成（见 §6.9），音频 WebAudio 合成 | 保证离线可构建、无版权问题 |

### 5.2 目录结构
```
/index.html               # 含 #app 容器、UI overlay 骨架、内联关键 CSS
/src/main.js              # 入口：装配所有系统，主循环
/src/config.js            # ★ 全部数据：调色板、道路表、别墅表、树表、预设参数
/src/core/renderer.js     # 渲染器、resize、pixelRatio 钳制
/src/core/loop.js         # 固定步长 update + 渲染帧循环，帧时间统计
/src/world/terrain.js     # 地面/沙滩/海岬岩台 + getGroundHeight()
/src/world/ocean.js       # 海面 Water + 岸线泡沫带
/src/world/skybox.js      # Sky + 太阳 + 光照装配 + 三预设切换
/src/world/roads.js       # 道路/人行道/路缘 挤出生成
/src/world/villa.js       # 程序化别墅生成器（核心，见 §6.4）
/src/world/landmarks.js   # 花石楼/公主楼/蝴蝶楼/元帅楼 定制装配
/src/world/trees.js       # 树木工厂 + InstancedMesh 布置
/src/world/props.js       # 路灯/院墙/绿篱/长椅/街牌/更衣棚/礁石
/src/systems/player.js    # PointerLock + WASD + 碰撞 + 触摸兜底
/src/systems/collision.js # 圆 vs AABB/圆 碰撞体注册与求解
/src/systems/atmosphere.js# 雾/预设过渡、落叶与海鸥粒子
/src/systems/audio.js     # WebAudio 合成氛围声
/src/systems/postfx.js    # EffectComposer: Render+Bloom+Output
/src/systems/ui.js        # 加载屏/开始按钮/HUD/地点提示/帮助
/src/systems/quality.js   # 自动降质（FPS 监测→关阴影/降分辨率）
/src/utils/textures.js    # Canvas 程序贴图工厂（墙面/瓦/花岗岩/沙/路/水法线）
/src/utils/rng.js         # 可种子化随机 (mulberry32)，全场景统一用它
```
- **模块契约**：每个 world/system 模块导出 `create*(ctx)` 返回 `{ group?, update?(dt, t), dispose?() }`，`main.js` 统一收集 update。`ctx` 内含 `{ scene, camera, renderer, config, rng, getGroundHeight, colliders, events }`。
- `events` 是一个极简 EventTarget（如 `preset-changed`、`enter-poi`），避免模块互相 import。

### 5.3 渲染管线
- `WebGLRenderer({ antialias: true })`；`outputColorSpace = SRGBColorSpace`；`toneMapping = ACESFilmicToneMapping, exposure = 0.9（黄昏）/1.0（晴午）/0.75（海雾）`。
- `setPixelRatio(Math.min(devicePixelRatio, 2))`。
- 后期：`EffectComposer` → `RenderPass` → `UnrealBloomPass(strength 0.22, radius 0.5, threshold 0.85)` → `OutputPass`。海雾模式 bloom strength 提到 0.45（路灯光晕）。**不加 SSAO**（性能预算内靠雾+阴影就够）。
- 阴影：仅太阳一个 `DirectionalLight` 投影，`shadow.mapSize 2048²`，正交范围 ±90 m **跟随玩家**（light.position/target 每帧随玩家平移并对齐到 texel 网格防闪烁）。`PCFSoftShadowMap`。路灯**不投影**。

---

## 6. 各子系统详细设计

### 6.1 天空与光照（`skybox.js`）
- 用 `three/addons/objects/Sky.js`：黄昏 `turbidity 8, rayleigh 2.2, mieCoefficient 0.008, mieDirectionalG 0.85`；晴午 `turbidity 4, rayleigh 1.2`；海雾模式天空几乎被雾色淹没（雾密度大时天空自然褪成雾色，另将 Sky 的 `rayleigh` 降到 0.5）。
- 光照三件套：
  - `DirectionalLight`（太阳）：黄昏 `#ffb36b`, intensity 2.6；晴午 `#fff4e0`, 3.2；海雾 `#cfd8da`, 0.9；
  - `HemisphereLight`：黄昏 sky `#7f97c9` / ground `#8a6a4d`, 0.55；晴午 `#bcd8f0`/`#6b7a50`, 0.8；
  - 环境反射：用 `RoomEnvironment` 不合适（室内），直接给材质统一 `envMapIntensity=0`，靠半球光即可（省一次 PMREM）。
- 云：8–12 个大尺度半透明白色 billboard 平面（程序化云贴图），高度 y≈120–180，缓慢平移，黄昏时被染橙（材质 color 随预设插值）。
- 预设切换用 1.5 s 参数插值过渡（雾密度/颜色、太阳位置/颜色、曝光、bloom、云色），切换期间不允许再次切换。

### 6.2 海洋（`ocean.js`）
- `three/addons/objects/Water.js`（平面镜像水），平面 700×400 置于 `y=0`，中心 `(0, 260)` 覆盖到雾界外。
- 水法线贴图**程序化生成**：512² Canvas 多倍频 value-noise 高度图 → Sobel 转法线（`utils/textures.js` 提供 `makeWaterNormalTexture()`），`RepeatWrapping`。
- 参数：黄昏 `waterColor #1a4f63, sunColor 同太阳色, distortionScale 2.4`；风向 uniform 随时间缓慢旋转 ±10°。
- **岸线泡沫**：沿沙滩水线放 3 条细长白色半透明平面（噪声 alpha 贴图），以 8 s 周期沿 z 平移 ±3 m + 透明度呼吸，模拟浪涌上滩；配合浪声包络同相位（audio 模块暴露 `wavePhase()`）。
- 礁石处放 2–3 个固定白色浪花环（低多边形环带 + 噪声 alpha，缓慢旋转）。

### 6.3 地形与道路（`terrain.js` / `roads.js`）
- 城区地面：一整块 `PlaneGeometry` 上色 `grass`，叠加程序草地贴图（低频色斑），非行走区域加 ±0.15 m 顶点噪声增加自然感（**路面与人行道单独成网格、绝对平整**）。
- 沙滩：独立条带网格按 §4.1 坡度插值，程序沙纹贴图；干湿分界——近水 4 m 颜色加深 12%。
- 海岬岩台：`CylinderGeometry`（8 边）压扁+顶点扰动成不规则岩台，侧面贴程序岩石贴图；周围 8–14 块 `IcosahedronGeometry(r, 1)` 顶点扰动礁石半浸水中。
- 道路生成：给定折线与宽度，逐段构建带状几何（两侧偏移 ±w/2，段间斜接），车行道/人行道/路缘三层。全部道路 merge 成 ≤6 个网格（按材质）。

### 6.4 程序化别墅生成器（`villa.js`，本项目工程量最大模块）
**输入**（每栋一条记录，固化在 `config.js`）：
```js
{ pos:[x,z], rotY, floors:2|3, w:10~16, d:8~13,          // 主体尺寸
  roof:'hip'|'gable'|'mansard', roofColorIdx, wallColorIdx,
  tower:null|{corner:0..3, round:bool},                   // 可选角塔
  porch:bool, balcony:bool, chimneys:1|2, graniteBase:true }
```
**生成规则**（BoxGeometry/ExtrudeGeometry 组合，全部 merge）：
1. 花岗岩墙基（高 0.9，四周比墙体外扩 0.08）；
2. 墙体逐层堆叠，层高 3.2；顶层若 mansard 则内收；
3. 红瓦屋顶：hip=四坡锥台、gable=三角挤出、mansard=折坡；挑檐 0.5；瓦纹用程序贴图（横向条纹+错缝）；
4. 窗：按立面均分开窗（间距≥2.2），窗=凹进 0.12 的深色玻璃面+白框（框用 4 根细 Box）；底层部分窗做拱顶（半圆环+矩形）；随机 40% 挂墨绿木百叶；**黄昏/海雾预设下随机 60% 窗发暖光**（emissive `glassDusk`，进 bloom）；
5. 门廊（porch）：2 柱+小坡檐；阳台：悬挑板+白栏杆；
6. 烟囱：屋脊附近 1–2 根，砖色，顶部出檐；
7. 材质仅 4 种共享：墙(每栋 tint 不同用 vertexColor 实现)、瓦、花岗岩、玻璃 → **每栋别墅 merge 后 ≤3 个 geometry，全场景别墅合计 ≤8 个 draw call**（按材质大合并，用 `BufferGeometryUtils.mergeGeometries` + 每栋先 `applyMatrix4`）。
8. 每栋注册一个 AABB 碰撞体（含 0.4 缓冲）；院墙另注册。

### 6.5 地标定制（`landmarks.js`，复用 villa 的部件函数）
- **花石楼（必做，投入最多细节）**：
  - 体量：主体 12×10×3 层（层高 3.4）+ 南侧**圆塔**（r=3，高出主体一层）+ 西侧多角形（八角半嵌）塔；
  - 全楼花岗岩贴图（程序：灰色乱石块+深缝，转角处色深 8% 模拟蘑菇石）；
  - **塔顶与主体檐口全部做雉堞垛口**（垛宽 0.8、口宽 0.5、高 0.6，用 Box 阵列，一段几何环形 instancing）；
  - 哥特尖拱窗（尖拱=两圆弧相交轮廓 ExtrudeGeometry），部分彩色玻璃（2–3 色 emissive 微光）；
  - 底部观景平台：石砌台+白宝瓶栏杆（栏杆柱 instancing），一段石阶下到沙滩；
  - 楼前一对花岗岩石尊（圆形花钵造型）。
- **公主楼（必做）**：墨绿外墙 `princessGreen`、陡坡屋顶（坡度 55°）、方形收分塔楼、白窗框、前院小花园；
- **蝴蝶楼/元帅楼（次优先）**：用 villa 生成器特化参数即可（蝴蝶楼：对称双翼+中央门廊；元帅楼：日式浅檐+格窗贴图），门口挂铭牌。

### 6.6 树木系统（`trees.js`）
- 每树种一个**工厂函数**返回 `{trunkGeo, canopyGeo, trunkMat, canopyMat}`，然后每树种 2 个 `InstancedMesh`（干/冠），实例含随机 `scale 0.85–1.25`、`rotY`、canopy 颜色微扰（`setColorAt` ±6% 明度）：
  - **银杏**：直干，冠=3–4 个上小下大的扁圆 Icosahedron 堆叠成宽扇形，纯金黄；树下地面叠一片金色落叶贴花（圆形渐变平面）；
  - **五角枫**：短干分叉（2–3 根斜 cylinder），冠=2–3 球，红橙渐变（顶点色上红下橙）；
  - **雪松**：塔形，5–7 层圆锥错叠，深绿，高 9–13 m；
  - **法桐**：高干（4 m 分叉），大伞冠，黄褐；干贴程序斑驳贴图（法桐特征）；
  - **黑松**：歪斜干（2 段折角），2–4 团扁平深绿伞状冠（海岸松特征）；
  - **龙柏**：柱状单锥，深绿；**碧桃/海棠/紫薇**：小乔木通用模板（矮干+1–2 球冠），按 §3.2 秋色区分。
- 布置：沿各路折线两侧、距路缘 1 m、间隔 7–9 m（银杏加密到 5.5 m 成隧道效果）；庭院树随别墅记录；海滨草坡撒黑松 25 棵；空地补雪松/法桐 30 棵。总量约 550 棵，**draw call = 树种数×2 ≈ 20**。
- 树干圆柱 6 段面、冠 Icosahedron detail=1，确保总面数可控。

### 6.7 街道家具与小品（`props.js`，全部 InstancedMesh 或 merge）
- **欧式路灯**：铸铁深绿灯柱（h=3.4，柱+弯臂+灯笼头），沿路每 25 m 交错布置，约 60 盏。黄昏/海雾亮灯：灯头 emissive + **假光晕 sprite**（径向渐变贴图，additive，海雾模式尺寸×2.2）；只有玩家最近的 2 盏挂真实 `PointLight`（池化复用，强度 6，距离 12）；
- 院墙/绿篱：随别墅生成（见 6.4）；街角**花岗岩街牌柱**：矮石柱+白底红字路名牌（Canvas 文字贴图，双面），路口共 14 块——既是真实细节又是玩家导航工具；
- 长椅（海滨黄海路沿线 6 张）、沙滩更衣棚（4 座条纹小屋）、临海白色宝瓶栏杆带（黄海路南缘连续 120 m，instancing）；
- 落叶贴花：银杏/枫树街整条路面撒 80 片单叶小平面（静态，随机旋转平铺）。

### 6.8 粒子与动态元素（`atmosphere.js`）
- **飘落叶**：200 个实例化小四边形（叶形 alpha 贴图，银杏黄/枫红两色），只在玩家半径 40 m 内激活；运动=缓慢下落+水平正弦漂摆+自转，落地淡出重生于树冠高度。风向与水面 uniform、树摇一致（全局 `wind` 对象）；
- **树冠微摆**：树 InstancedMesh 的材质注入极简 vertex shader 补丁（`onBeforeCompile`，按世界坐标+时间做 0.03 幅度顶点摆动，仅冠）；
- **海鸥**：10 只，2 个三角面拍翅（morph 或 shader 摆动），沿海岬上空 2 条椭圆航线巡飞，随机切换，偶发俯冲；与鸥鸣音效触发联动；
- 海雾模式附加：近地 8 片大尺寸半透明雾片 billboard 缓慢漂移（叠在 FogExp2 上做层次）。

### 6.9 程序化贴图工厂（`utils/textures.js`）
统一模式：`make*(size=512) → CanvasTexture`（`colorSpace = SRGBColorSpace`，法线图除外）。需要提供：
`makeStucco()`（拉毛墙面噪点）、`makeRoofTiles()`（横条瓦纹+错缝高光线）、`makeGranite()`（乱石块+砂浆缝，花石楼用 1024）、`makePlaneBark()`（法桐斑驳）、`makeAsphalt()`、`makeSand()`、`makeGrass()`、`makeWaterNormalTexture()`、`makeLeafAlpha(shape)`（银杏扇形/枫五角）、`makeGlowSprite()`、`makeCloud()`、`makeSignText(text)`（街牌）、`makeFoamStrip()`。
全部基于 `rng.js` 的种子噪声（value noise + fbm 工具函数放同文件），**禁止使用外部图片**。

### 6.10 玩家控制与碰撞（`player.js` / `collision.js`）
- `PointerLockControls`；点击「开始游览」进入锁定；`Esc` 弹出帮助/继续覆盖层；
- 移动：WASD/方向键，走 3.2 m/s，Shift 跑 6 m/s；加速度 25 m/s²、阻尼 10/s（有惯性但不打滑）；眼高 = `getGroundHeight()+1.7`，上下坡平滑（每帧对地面高度 lerp 0.2）；轻微步行摆动（视线 y 正弦 ±0.035 m，跑步频率×1.6，可在 config 关）；
- 碰撞：玩家=半径 0.5 圆（俯视 2D），碰撞体=静态 AABB（建筑/院墙/更衣棚）与圆（树干 r=0.35、灯柱、街牌）。求解：先宽相（空间哈希网格 8 m 格），后窄相推挤（沿法线滑动，不卡墙角——AABB 用最小平移向量，圆用径向推出）；世界边界 `x±185, z∈[-165, …]`；**海滩可走**，走进水线 `z > 水深0.4m 线` 用不可见墙挡住（沿岸 poly 碰撞条）；花石楼石阶用 2 段斜坡高度区（写进 getGroundHeight 的特例）；
- 触摸兜底（次优先）：检测 `pointer: coarse` → 显示左侧虚拟摇杆（自绘 2 个 div，pointer 事件）+ 右半屏拖动转视角；不做也不算验收失败，但要保证 UI 提示「建议桌面体验」。

### 6.11 UI / HUD（`ui.js`，纯 DOM+CSS，不进 WebGL）
- **加载屏**：深蓝渐变底 + 标题「青島 · 八大關」（衬线字体 serif fallback）+ 副标「紅瓦綠樹 · 碧海藍天」+ 进度条（按模块初始化步数）→ 完成后变「开始游览」按钮；
- **HUD（常驻，极简）**：左下当前所在路名（由玩家坐标对道路距离判定，斜 8° 白字）；右下按键提示图标（WASD/Shift/1-2-3/Esc），10 s 无操作淡出；
- **地点提示（POI toast）**：进入触发半径显示 2.5 s 大字标题+一行史实说明，每个 POI 只弹一次（会话内）。6 个 POI：
  1. 花石楼（r=18）「1930年代花岗岩古堡式别墅，蒋介石曾下榻」
  2. 公主楼（r=14）「丹麦童话风格，居庸关路10号」
  3. 蝴蝶楼（r=14）「1934年，影星胡蝶在此拍摄《劫后桃花》」
  4. 居庸关路银杏道（r=10，点 (0,-30)）「一关一树：居庸关路的银杏」
  5. 第二海水浴场（r=25，点 (-60,90)）
  6. 元帅楼（r=14）
- 预设切换按钮（右上 3 个小图标，同时支持键 1/2/3）；
- 移动端提示条。所有文案简体中文（标题可用繁体做装饰）。

### 6.12 性能与自动降质（`quality.js`）
**预算**：draw calls ≤ 250；三角形 ≤ 120 万；显存贴图 ≤ 40 MB；主线程 update ≤ 3 ms/帧。
| 手段 | 说明 |
|---|---|
| 合并/实例化 | 别墅按材质大合并；树/灯/栏杆/垛口 instancing（上文已定） |
| 阴影 | 仅太阳 2048²，±90 m 跟随；`matrixAutoUpdate=false` 的静物不更新 |
| 视锥+距离 | 相机 far=450（雾里看不到那么远）；落叶/海鸥 40–120 m 外停更新 |
| 自动降质 | 滚动 3 s 平均 FPS < 45：先 pixelRatio→1.25，再 bloom 关，再阴影 1024；恢复迟滞 15 s。HUD 不通知，静默进行 |
| 移动端初始 | `pointer: coarse` 时直接从 pixelRatio 1.25 + 阴影 1024 起步 |

---

## 7. 主循环与装配（`main.js` 伪代码）
```js
const ctx = createContext();            // renderer, scene, camera, config, rng, events…
await ui.showLoading(async step => {
  step('地形',   () => terrain.create(ctx));
  step('海洋',   () => ocean.create(ctx));
  step('天空',   () => skybox.create(ctx));
  step('道路',   () => roads.create(ctx));
  step('别墅',   () => villas.create(ctx));      // 读 config.villas 表
  step('地标',   () => landmarks.create(ctx));
  step('树木',   () => trees.create(ctx));
  step('小品',   () => props.create(ctx));
  step('氛围',   () => atmosphere.create(ctx));
  step('后期',   () => postfx.create(ctx));
});
ui.onStart(() => { player.lock(); audio.start(ctx); });
loop.run((dt, t) => { player.update(dt); [ocean, skybox, atmosphere, props, quality, ui].forEach(s => s.update?.(dt, t)); postfx.render(); });
```
- 初始化必须**分帧执行**（每步 `await nextFrame()`），避免长任务卡加载动画；
- 所有几何生成在初始化期完成，运行期**零 new 几何/材质**。

---

## 8. 关键实现风险与对策
| 风险 | 对策 |
|---|---|
| Water.js 镜像水在大场景反射开销大 | 反射 RT 固定 512²；海雾模式反射可见度低，可将 RT 降 256² |
| 程序贴图丑 → 氛围崩 | textures.js 先行开发+单独预览页（`/texture-lab.html` dev 专用）逐张调参 |
| 别墅生成器窗洞对不齐/穿模 | 全部尺寸走 0.1 m 网格取整；先做 1 栋「样板房」四方向截图核对再批量 |
| 合并后拾取/调试难 | dev 模式（`?debug`）提供 wireframe 开关+碰撞体可视化+FPS/drawcall 面板（自绘，不引库） |
| 树 too low-poly 显廉价 | 冠部顶点噪声扰动+顶点色渐变（下暗上亮）+微摆动，比加面数更有效 |
| PointerLock 在 iframe/Safari 受限 | 捕获失败时自动退回「拖拽视角+WASD」模式并提示 |

---

## 9. 明确排除项（防止实现跑偏）
- ❌ WebGPU / TSL、❌ 任何 glTF/纹理下载、❌ 物理引擎（自写 2D 碰撞足够）、❌ 昼夜连续循环（只有 3 个离散预设）、❌ 室内场景、❌ NPC/行人、❌ 小地图（街牌承担导航）、❌ i18n（只做中文）。

## 10. 验收清单（实现完成后逐条核对）
详见 [todo.md](./todo.md) 第 11 阶段，其中「氛围验收」9 条是硬标准。

## 11. 给实现 agent 的开工顺序建议
严格按 todo.md 阶段推进；**Phase 2（贴图工厂）和 Phase 4（样板别墅）是质量地基，宁慢勿糙**；每个 Phase 结束跑一次 `npm run build` 确保可构建，并截图存 `/screenshots/phase-N.png` 备查。

## 12. 调研来源
- [维基百科：八大关](https://zh.wikipedia.org/zh-cn/%E5%85%AB%E5%A4%A7%E5%85%B3)（道路构成、万国建筑、花石楼/公主楼位置）
- [青岛新闻网：八大关的秘密](https://news.qingdaonews.com/wap/2017-04/06/content_11995627.htm)（一关一树树种映射）
- [百度百科：青岛花石楼](https://baike.baidu.com/item/%E9%9D%92%E5%B2%9B%E8%8A%B1%E7%9F%B3%E6%A5%BC/2611997)、[搜狐：花石楼三面临海](https://www.sohu.com/a/535260461_397390)（花岗岩砌墙、雉堞女儿墙、塔楼造型、历史）
- [澎湃：青岛20栋绝美老建筑](https://www.thepaper.cn/newsDetail_forward_18297391)（公主楼/蝴蝶楼/元帅楼风格与地址）
- [大众网：八大关——红瓦绿树碧海蓝天](https://www.dzwww.com/2013/sdwsdt/qdwsdt/qdsqwsdt/qdsqzryc/201312/t20131222_9382808.htm)（整体色彩意象）
- [three.js Releases](https://github.com/mrdoob/three.js/releases) / [npm: three](https://www.npmjs.com/package/three)（版本 0.185.0 确认）
