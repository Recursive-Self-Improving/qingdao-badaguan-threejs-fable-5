# Lessons

- This Vite app is configured for GitHub Pages project deployment at `/qingdao-badaguan-threejs-fable-5/`; keep `vite.config.js` `base` aligned with the repository path unless deploying to a custom/root domain.
